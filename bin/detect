#!/usr/bin/env bash
set -eo pipefail

TSCONFIG_FLAG=""
echo "Detecting Deno application..."
if [[ -f tsconfig.json ]]; then
  TSCONFIG_FLAG="-c ${TSCONFIG}"
  echo "---> Using TypeScript config ${TSCONFIG}"
fi
